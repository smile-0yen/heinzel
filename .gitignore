# Local operator state — never belongs in the repository
.heinzel/
*.local
etc/*.conf
!etc/*.conf.in
!etc/heinzel.conf.example

# Generated from templates at install time
etc/*.plist
etc/pf-travel.conf
etc/sudoers-diag
etc/sudoers-ticket

# macOS
.DS_Store
etc/heinzel-settings.json

# Byte-compiled by python3 when `hzl web` serves the dashboard
__pycache__/
